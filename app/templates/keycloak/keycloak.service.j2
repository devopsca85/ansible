[Unit]
Description=Keycloak
After=network.target
 
[Service]
Type=idle
User=root
Group=root

Environment="JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64/"

ExecStart=/opt/services/keycloak/bin/standalone.sh -b 127.0.0.1
TimeoutStartSec=600
TimeoutStopSec=600

[Install]
WantedBy=multi-user.target
